# Database
# For Docker Compose use DB_HOST=db; for local host Postgres use DB_HOST=localhost
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=appdb
DB_SSLMODE=disable
DB_DIALECT=postgres

# Auth
JWT_SECRET=change-me-please
JWT_TTL_HOURS=24
